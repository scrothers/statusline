// Package demo provides canned statusline scenarios — synthetic payloads
// and git status, not real Claude Code JSON — so the demo command can
// render representative output without any external input.
package demo
