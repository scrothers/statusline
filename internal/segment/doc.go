// Package segment defines the Segment interface and every concrete
// statusline segment (model, directory, git, context window, cost, rate
// limits, and low-priority bonus badges), each rendering itself into
// style.Chunks from a RenderContext.
package segment
