// Package theme resolves the five built-in named color palettes (Gruvbox,
// Catppuccin Mocha, Tokyo Night, Nord, Dracula) that segments render
// against, plus the shared Nerd Font icon table with plain-text fallbacks.
package theme
